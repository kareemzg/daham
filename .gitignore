# Godot
.godot/
*.tmp
# Presets are the build itself and belong in the repo; the credentials file
# beside them holds passwords and does not.
export_credentials.cfg
/android/
/ios/

# Exported builds
build/

# macOS
.DS_Store

# Test renders and pipeline scratch output
tools/out/
tools/pipeline/.venv/
tools/pipeline/__pycache__/
tools/pipeline/raw/
